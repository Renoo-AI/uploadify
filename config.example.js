// config.example.js
// IMPORTANT: Copy this file to 'config.js' and fill in your actual details.
// DO NOT commit your actual config.js to public repositories!

const GITHUB_CONFIG = {
    // Your personal access token (needs 'repo' scope)
    TOKEN: "ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",

    // The username or organization that owns the repo
    OWNER: "your-github-username",

    // The name of the repository to store files in
    REPO: "your-repo-name",

    // The branch to push files to
    BRANCH: "main"
};
